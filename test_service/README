Test Service
============

This app has a /whoami controller which will respond to either
html, json, or xml.

Installation
------------
1. [OPTIONAL] Set your CAS server URL in config/environments/development.rb

      example.
        central( { :cas => { :cas_base_url => 'http://localhost/cas' } } )