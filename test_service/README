Test Service
============

This app has a /whoami controller which it will respond with either
html, json, or xml.

Installation
------------
1. Set your CAS server URL in config/environments/development.rb
  example.
    central( { :cas => { :cas_base_url => 'http://localhost/cas' } } )